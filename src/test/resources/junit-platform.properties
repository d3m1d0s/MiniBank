# Registers every extension declared in META-INF/services for the whole suite, which is how
# SecurityContextCleanupExtension applies without each test class having to opt in.
junit.jupiter.extensions.autodetection.enabled=true
